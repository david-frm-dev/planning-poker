# blueprint-angular

This is a ANGULAR BLUEPRINT with NX workspace.

run the application with `cd planning-poker-backend && ./gradlew bootRun`.

for hot reloading of the frontend, use `cd planning-poker && npm run dev`